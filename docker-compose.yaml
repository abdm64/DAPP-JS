version: "3.3"

services:
  eath-app:
    build:
      context: ./app
      dockerfile: Dockerfile
    environment: 
    #   - ACCOUNT=
    #   - AMOUNT=
        - TIME= 12:04
    #   - INFURA_ID= 
    #   - SLACK_TOKEN=
    #   - SLACK_CHAT_ID=
    #   - TELEGRAM_TOKEN=
    #   - TELEGRAM_CHAT_ID=   

     # Please notice in order o get the applcaion work you need to fill env var 